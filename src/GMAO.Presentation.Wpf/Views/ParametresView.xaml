<UserControl
    x:Class="GMAO.Presentation.Wpf.Views.ParametresView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:conv="clr-namespace:GMAO.Presentation.Wpf.Converters"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Background="{StaticResource PinceauFondHud}"
    FontFamily="Segoe UI">
    <UserControl.Resources>
        <conv:StringVersVisibiliteConverter x:Key="StrVisib" />
        <Style x:Key="Carte" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource PinceauSurfaceHud}" />
            <Setter Property="BorderBrush" Value="{StaticResource PinceauSurfaceHudBordure}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="6" />
            <Setter Property="Padding" Value="22" />
            <Setter Property="Margin" Value="0,0,0,18" />
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect
                        BlurRadius="12"
                        Direction="270"
                        ShadowDepth="2"
                        Color="#1A000000" />
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="TitreSection" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Foreground" Value="{StaticResource PinceauTexteHud}" />
            <Setter Property="Margin" Value="0,0,0,14" />
        </Style>
        <Style x:Key="Cle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#78909C" />
            <Setter Property="Margin" Value="0,4" />
        </Style>
        <Style x:Key="Valeur" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource PinceauTexteHud}" />
            <Setter Property="Margin" Value="12,4" />
        </Style>
    </UserControl.Resources>

    <ScrollViewer Padding="28,24" VerticalScrollBarVisibility="Auto">
        <StackPanel MaxWidth="760" HorizontalAlignment="Left">

            <!--  Profil  -->
            <ui:CardExpander
                Margin="0,0,0,14"
                Header="Profil"
                IsExpanded="True">
                <StackPanel>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="140" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition />
                            <RowDefinition />
                            <RowDefinition />
                            <RowDefinition />
                        </Grid.RowDefinitions>
                        <TextBlock
                            Grid.Row="0"
                            Grid.Column="0"
                            Style="{StaticResource Cle}"
                            Text="Nom complet" />
                        <TextBlock
                            Grid.Row="0"
                            Grid.Column="1"
                            FontWeight="SemiBold"
                            Style="{StaticResource Valeur}"
                            Text="{Binding UtilisateurNom}" />
                        <TextBlock
                            Grid.Row="1"
                            Grid.Column="0"
                            Style="{StaticResource Cle}"
                            Text="Identifiant" />
                        <TextBlock
                            Grid.Row="1"
                            Grid.Column="1"
                            FontFamily="Cascadia Code, Consolas"
                            Style="{StaticResource Valeur}"
                            Text="{Binding Login}" />
                        <TextBlock
                            Grid.Row="2"
                            Grid.Column="0"
                            Style="{StaticResource Cle}"
                            Text="Rôle" />
                        <TextBlock
                            Grid.Row="2"
                            Grid.Column="1"
                            Style="{StaticResource Valeur}"
                            Text="{Binding RoleLibelle}" />
                        <TextBlock
                            Grid.Row="3"
                            Grid.Column="0"
                            Style="{StaticResource Cle}"
                            Text="E-mail" />
                        <TextBlock
                            Grid.Row="3"
                            Grid.Column="1"
                            Style="{StaticResource Valeur}"
                            Text="{Binding Email}" />
                    </Grid>
                </StackPanel>
            </ui:CardExpander>

            <!--  Sécurité  -->
            <ui:CardExpander
                Margin="0,0,0,14"
                Header="Sécurité"
                IsExpanded="True">
                <StackPanel>
                    <TextBlock
                        Margin="0,0,0,4"
                        Style="{StaticResource Cle}"
                        Text="Mot de passe actuel" />
                    <PasswordBox
                        x:Name="PbAncien"
                        Height="36"
                        Margin="0,0,0,10"
                        Padding="8,0"
                        VerticalContentAlignment="Center"
                        Background="#0E141B"
                        BorderBrush="#2E3A44"
                        Foreground="{StaticResource PinceauTexteHud}" />
                    <TextBlock
                        Margin="0,0,0,4"
                        Style="{StaticResource Cle}"
                        Text="Nouveau mot de passe (Min. 8 caractères, 1 majuscule, 1 minuscule, 1 chiffre)" />
                    <PasswordBox
                        x:Name="PbNouveau"
                        Height="36"
                        Margin="0,0,0,10"
                        Padding="8,0"
                        VerticalContentAlignment="Center"
                        Background="#0E141B"
                        BorderBrush="#2E3A44"
                        Foreground="{StaticResource PinceauTexteHud}" />
                    <TextBlock
                        Margin="0,0,0,4"
                        Style="{StaticResource Cle}"
                        Text="Confirmer le nouveau mot de passe" />
                    <PasswordBox
                        x:Name="PbConfirme"
                        Height="36"
                        Margin="0,0,0,12"
                        Padding="8,0"
                        VerticalContentAlignment="Center"
                        Background="#0E141B"
                        BorderBrush="#2E3A44"
                        Foreground="{StaticResource PinceauTexteHud}" />
                    <Button
                        Padding="14,8"
                        HorizontalAlignment="Left"
                        Background="#00E5A8"
                        BorderThickness="0"
                        Command="{Binding ChangerMotDePasseCommand}"
                        Content="Modifier le mot de passe"
                        Cursor="Hand"
                        FontWeight="SemiBold"
                        Foreground="#0A0E12" />
                    <TextBlock
                        Margin="0,12,0,0"
                        Foreground="#29B6F6"
                        Text="{Binding MessageMotDePasse}"
                        TextWrapping="Wrap"
                        Visibility="{Binding MessageMotDePasse, Converter={StaticResource StrVisib}}" />
                </StackPanel>
            </ui:CardExpander>

            <!--  Historique des connexions  -->
            <ui:CardExpander Margin="0,0,0,14" Header="Historique des connexions">
                <StackPanel>
                    <ItemsControl ItemsSource="{Binding Historique}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="0,4">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="160" />
                                        <ColumnDefinition Width="90" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock
                                        Grid.Column="0"
                                        FontFamily="Cascadia Code, Consolas"
                                        FontSize="12"
                                        Foreground="{StaticResource PinceauTexteHud}"
                                        Text="{Binding DateConnexion, StringFormat=g}" />
                                    <Border
                                        Grid.Column="1"
                                        Padding="8,2"
                                        HorizontalAlignment="Left"
                                        CornerRadius="9">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Background" Value="#1A00E676" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Succes}" Value="False">
                                                        <Setter Property="Background" Value="#22FF3B30" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <TextBlock
                                            FontSize="11"
                                            FontWeight="SemiBold"
                                            Text="{Binding Statut}">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Foreground" Value="#00E676" />
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Succes}" Value="False">
                                                            <Setter Property="Foreground" Value="#FF6B60" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </Border>
                                    <TextBlock
                                        Grid.Column="2"
                                        Margin="8,0,0,0"
                                        VerticalAlignment="Center"
                                        FontSize="12"
                                        Foreground="#90A4AE">
                                        <Run Text="{Binding AdresseIp, Mode=OneWay}" /><Run Text="  " /><Run Text="{Binding Detail, Mode=OneWay}" />
                                    </TextBlock>
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </ui:CardExpander>

            <!--  À propos  -->
            <ui:CardExpander Margin="0,0,0,14" Header="À propos">
                <StackPanel>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="160" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition />
                            <RowDefinition />
                            <RowDefinition />
                            <RowDefinition />
                        </Grid.RowDefinitions>
                        <TextBlock
                            Grid.Row="0"
                            Grid.Column="0"
                            Style="{StaticResource Cle}"
                            Text="Application" />
                        <TextBlock
                            Grid.Row="0"
                            Grid.Column="1"
                            Style="{StaticResource Valeur}"
                            Text="MEDICANA Datex-Ohmeda" />
                        <TextBlock
                            Grid.Row="1"
                            Grid.Column="0"
                            Style="{StaticResource Cle}"
                            Text="Version" />
                        <TextBlock
                            Grid.Row="1"
                            Grid.Column="1"
                            FontFamily="Cascadia Code, Consolas"
                            Style="{StaticResource Valeur}"
                            Text="{Binding Version}" />
                        <TextBlock
                            Grid.Row="2"
                            Grid.Column="0"
                            Style="{StaticResource Cle}"
                            Text="Base de données" />
                        <TextBlock
                            Grid.Row="2"
                            Grid.Column="1"
                            FontFamily="Cascadia Code, Consolas"
                            Style="{StaticResource Valeur}"
                            Text="{Binding CheminBase}"
                            TextWrapping="Wrap" />
                        <TextBlock
                            Grid.Row="3"
                            Grid.Column="0"
                            Style="{StaticResource Cle}"
                            Text="Serveur notifications" />
                        <TextBlock
                            Grid.Row="3"
                            Grid.Column="1"
                            FontFamily="Cascadia Code, Consolas"
                            Style="{StaticResource Valeur}"
                            Text="{Binding ServeurNotifications}" />
                    </Grid>
                </StackPanel>
            </ui:CardExpander>

        </StackPanel>
    </ScrollViewer>
</UserControl>
